name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  #schedule:
    #- cron: '0 */1 * * *' # Runs every 4 hours

jobs:
  test:
    timeout-minutes: 60
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      run: npx playwright test
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
    - name: Send email with test results
      if: always()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp-relay.brevo.com
        server_port: 587
        username: 841d57002@smtp-brevo.com
        password: xsmtpsib-4f0bcc1e2f16cc3d67b475d6546b50fee87dd6173961afb5b4defdd54d78898b-6rs2zgWySTRZ8XON
        subject: Playwright Test Results
        to: vasalanga@traxiontech.net
        from: 841d57002@smtp-brevo.com
        body: |
          The Playwright tests have completed. Please find the attached report.
        attachments: playwright-report/index.html


  